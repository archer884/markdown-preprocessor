## A guide for agents re: Markdown Preprocessor

This application, the markdown preprocessor (called "mdp"), is intended to make certain edits to markdown files before they are passed into pandoc. Specifically, it needs to remove comments and convert double dashes into m-dashes. At present, it is failing to remove comments because reasons.

We need to find those reasons and correct them.
